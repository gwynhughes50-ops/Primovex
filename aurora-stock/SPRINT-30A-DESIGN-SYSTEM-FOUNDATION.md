# Sprint 30A - Primovex Design System Foundation

## Scope

Shared UI foundation only. No business logic, routing, Firebase, Pulse, Login, Inventory permissions, Mobile navigation or Tauri changes.

## Changed

- Standardised `PageHeader`, `SectionCard` and `EmptyState`
- Added reusable `StatCard`, `ActionBar` and `FormField`
- Updated shared `Input`, `Card` and `Tabs` to use semantic theme classes
- Rewrote the design-system documentation around Primovex and current theme tokens

## Regression gates

- Production build
- Primovex branding unchanged
- Pulse files unchanged
- Login files unchanged
- Inventory page unchanged
- Auth and Firebase unchanged
- Mobile routing unchanged
- Tauri configuration unchanged
